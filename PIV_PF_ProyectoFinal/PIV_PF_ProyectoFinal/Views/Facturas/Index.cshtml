@model IEnumerable<PIV_PF_ProyectoFinal.Models.Factura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetodoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdClientesNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetodoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdClientesNavigation.IdClientes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodigoFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodigoFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodigoFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
